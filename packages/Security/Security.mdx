import { Canvas, Meta } from "@storybook/blocks";

import * as Security from "./Security.stories";

<Meta of={Security} />

# Security

The `Security` component displays a button with a lock icon and text. It triggers a callback when clicked.

## Usage

```jsx
import Security from "./Security";

function MyComponent() {
  return <Security onClick={() => console.log("Button clicked!")} />;
}
```

<Canvas of={Security.Default} />
