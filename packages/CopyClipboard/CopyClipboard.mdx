import { Meta, Canvas } from "@storybook/addon-docs/blocks";

import * as CopyClipboard from "./CopyClipboard.stories";

<Meta of={CopyClipboard} />

# CopyClipboard

This component contains a "copy to clipboard" button. When the button is clicked, the text gets copied to the user's clipboard.

##Usage

- `clipboardText`: Specifies the text to be shown and copied.

## Story

<Canvas of={CopyClipboard.Default} />
