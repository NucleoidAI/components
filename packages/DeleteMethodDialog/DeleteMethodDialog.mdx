import { Meta, Canvas } from "@storybook/addon-docs/blocks";

import * as DeleteMethodDialog from "./DeleteMethodDialog.stories";

<Meta of={DeleteMethodDialog} />

# DeleteMethodDialog

This component shows a dialog for confirming the delete operation. It has two main functions: one for controlling the dialog visibility (`setOpen`) and another for executing the delete operation (`deleteMethod`).

## Story

<Canvas of={DeleteMethodDialog.Default} />
