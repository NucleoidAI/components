import { Canvas, Meta, Story } from "@storybook/blocks";

import * as ParamView from "./ParamView.stories";

<Meta of={ParamView} />

# ParamView

`ParamView` is a table component that displays a list of parameters and their data types. It accepts `params` which is an array of objects with `name` and `type`.

## Usage

<Canvas of={ParamView.Default} />
