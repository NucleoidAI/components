import { Meta, Canvas } from "@storybook/addon-docs/blocks";

import * as AlertMessage from "./AlertMessage.stories";

<Meta of={AlertMessage} />

# AlertMessage

This component shows an alert message that will disappear after a certain duration.

### Default

<Canvas of={AlertMessage.Default} />
