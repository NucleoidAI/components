import { Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as SmallLogoStories from "./SmallLogo.stories";

<Meta of={SmallLogoStories} />

# SmallLogo

The SmallLogo is a reusable React component.

To use the SmallLogo, import it into your desired file and include it in your JSX.

<Canvas of={SmallLogoStories.Default} />
